# Django conversion



```bash
python -m pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Endpoints similar to original:
- `POST /subjects/`
- `POST /teachers/`
- `POST /classes/`
- `POST /students/`
- `POST /schedules/`
- `POST /grades/`
